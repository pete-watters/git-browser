<h3>Login</h3>

<form {{action 'authenticate' on='submit'}}>
  <label for="githubToken">Login</label>
  {{input id='githubToken' placeholder='Enter githubToken' value=githubToken}}
   <button type="submit">Login</button>
</form>
