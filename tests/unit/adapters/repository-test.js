import {expect} from 'chai';
import {it, describe} from 'mocha';
import {setupTest} from 'ember-mocha';

describe('Unit | Adapter | repository', function() {
    setupTest('adapter:repository', {

    });

    // it('exists', function() {
    //   let route = this.subject();
    //   expect(route).to.be.ok;
    // });
});
